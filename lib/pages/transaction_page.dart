import 'package:flutter/material.dart';

class TransactionPage extends StatelessWidget {
  TransactionPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text("TransactionPage")));
  }
}
